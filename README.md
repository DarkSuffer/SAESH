`local VERSION = "1.8.5"`
```ini
[*] Bug Fixes & Architecture (CRITICAL)
  [+] resolved 'attempt to call a nil value' crash on load
  [+] safeParentGui function scope moved before invocation
  [+] makeScriptCard / renderServerCard re-architected with explicit parents
  [+] full UI tree, Settings, and Script Hub restored in scope-safe blocks
```
