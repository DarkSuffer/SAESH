# 🥚 StealEgg Finder · v1.6.1
**Server Finder + Script Hub** · by `@jo_2527`

## ✨ What’s New
• **Keyless Scripts** section (Script Hub + Auto-Exec)
  → `ZEROIN HUB` · `Foxname Hub` · `Lennon Hub`
• Clean **Fyy-style webhooks** (no wall of text)
• **Join / Copy / Discord** buttons + markdown fallback
• Player count in title → `1-Player · 1/7`
• Ping controls: `@here` / `@everyone` / OFF
• Any-match alerts (2–6p) toggle
• **Fake test webhook** (example links only)
• JobId joiner + paste · Export all · Copy my JobId
• Universal join (Mobile + PC)
  `roblox.com/games/start?placeId=&gameInstanceId=`
• Toasts · star ratings · themes · low-spec mode

## 🔧 Fixes
• Webhook buttons more reliable (`wait=true`)
• Components fallback if Discord strips them
• Fixed old `launchData` join links
• Layout / canvas / hop persist polish

## ⚙️ Quick Setup
1. Settings → paste webhook
2. Set **1-Player Ping** (`@here` recommended)
3. Optional: enable **Any-Match Alerts**
4. Hit **TEST WEBHOOK** (fake data only)
5. Scan → get clean alerts + join buttons

## 📎 Notes
• Hop persist: set `SCRIPT_URL` **or** save as
  `StealEgg_ServerFinder.lua`
• Discord may hide buttons on some webhooks —
  markdown Join/Copy links still show

`v1.6.1` ·
